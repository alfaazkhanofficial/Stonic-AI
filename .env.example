# STONIC provider credentials. Never commit a real key.
# xKiro OpenAI-compatible gateway (default: https://api.xkiro.com/v1).
# XKIRO_API_KEY=
# STONIC_LLM_API_KEY=   # optional generic override for a custom OpenAI-compatible endpoint
# Optional development data location (defaults to ./data).
# STONIC_DATA_DIR=
